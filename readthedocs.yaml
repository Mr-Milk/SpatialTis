version: 2

submodules:
  include:
    - tutorial

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: false

python:
  version: 3.8
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - all